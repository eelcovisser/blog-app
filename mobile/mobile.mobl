application mobile

import mobl::ui::generic

screen root() {
	var title = "blog"
	header(title)
	var blog = async(Blog.data())
	whenLoaded(blog) {
		label(blog.about)
		label(blog.description)
	}
}

service Blog {
	resource data(): JSON {
		uri = "/blog/apiblog"
	}
}
 