application mobile

import mobl::ui::generic
import blogmodel
import blogview

screen root() {
	var title = "blog"
	header(title)
	var posts = async(Blog.recentposts())
	whenLoaded(posts) {
	  list(post in posts) {
	    item{ label(post.title) }
	  }
	}
}



